# My notes

```
rm -f main.rom && ../../wla-dx/build/binaries/wla-z80 -o main.o main.asm && ../../wla-dx/build/binaries/wlalink linkfile main.rom
/opt/openMSX/bin/openmsx -machine Gradiente_Expert_GPC-1 -cart ~/code/tweaking/assembly101/03-soma/main.rom
```
